from src.bot.actions.base import BaseAction


class SingleDeviceSpyGameProceedAction(BaseAction, prefix="single_device_spy_game_proceed"):
    """
    Callback action for proceeding to the next player in a single-device spy game.
    """
