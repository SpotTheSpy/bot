from src.bot.actions.base import BaseAction


class SingleDeviceSpyGamePlayAgainAction(BaseAction, prefix="single_device_spy_game_play_again"):
    """
    Callback action for playing again a new single-device spy game with the same setup.
    """
